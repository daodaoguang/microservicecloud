spring:
  cloud:
    config:
      name: microservicecloud-config-eureka-client     #需要从 github上读取的 资源名称 ，注意没有yml后缀名
      profile: dev   #本次访问的配置项   里面的profile值是什么，决定从github上读取什么  test  dev 都默认在github上对应的端口就是7001
      label: master
      uri: http://config-3344.com:3344      #SpringCloudConfig获取的服务地址

